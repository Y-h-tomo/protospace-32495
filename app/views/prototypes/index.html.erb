<main class="main">
  <div class="inner">
    <% if user_signed_in? %>
       <div class="greeting">
          　こんにちは、
         <%= link_to "#{current_user.name}さん", "/users/#{current_user.id}", class: :greeting__link%>
      </div>
    <% end %>
    <div class = "card__wrapper">
         <%# プロトタイプ部分テンプレートを変数を渡して複数表示%>
    　 <%= render partial: 'prototype', collection: @prototypes %>
    </div>
  </div>
</main>
